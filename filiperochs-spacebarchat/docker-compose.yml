services:
  app_proxy:
    environment:
      # proxy deve apontar para o serviço web (Fermi)
      APP_HOST: filiperochs-spacebarchat_fermi_1
      APP_PORT: 5236
      PROXY_AUTH_ADD: "false"

  fermi:
    image: filiperochs/spacebarchat_client_fermi
    container_name: filiperochs-spacebarchat_fermi_1
    restart: on-failure
    environment:
      - NODE_ENV=production
      - PORT=5236
